{
    "name": "demo",
    "version": "0.1.0",
    "scripts": {
        "dev": "polymer serve | npm run watch",
        "test": "polymer test",
        "watch": "browser-sync start --proxy http://127.0.0.1:8081 --files \"src/**/*.*, index.html, *.js\" --no-notify"
    }
}
